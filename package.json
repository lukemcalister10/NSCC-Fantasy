{
  "name": "nscc-fantasy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Club fantasy cricket platform — computational engine core (scoring, pricing, cap ledger).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
