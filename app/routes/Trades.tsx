import { Placeholder } from "../components/Placeholder";

/**
 * Buy/sell. Stub — built by the team/trade UI slice (S-C). Reachable by URL
 * only: it carries no nav entry, so S-C is free to place trades as a tab inside
 * /team rather than a peer of it.
 */
export function Trades() {
  return <Placeholder title="Trades" />;
}
