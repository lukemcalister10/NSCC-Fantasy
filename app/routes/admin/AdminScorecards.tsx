import { Placeholder } from "../../components/Placeholder";

/** Scorecard entry + list. Stub — built by the manager-core slice (S-A). */
export function AdminScorecards() {
  return <Placeholder title="Scorecards" />;
}
