import { Placeholder } from "../../components/Placeholder";

/** Player registry. Stub — built by the manager-core slice (S-A). */
export function AdminPlayers() {
  return <Placeholder title="Player registry" />;
}
