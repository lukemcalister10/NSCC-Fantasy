import { Placeholder } from "../../components/Placeholder";

/** Config + season lock. Stub — built by the manager-core slice (S-A). */
export function AdminSettings() {
  return <Placeholder title="Settings" />;
}
